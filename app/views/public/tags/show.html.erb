<div class="container">
  <div class="row">
    <div class="titleK">
      <h1><%= @tag.name %></h1>
    </div>
  </div>
  <div class="row pt-5">
    <% @tag.posts.each do |post| %>
      <div class="col-md-4 mb-3">
        <div class="card">
          <div class="card-body">
            <%= link_to post.post_content.truncate(20), post_path(post) %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>